create table vehicles(
    id varchar primary key ,
    threadId varchar,
    lineId varchar,
    name varchar,
    type varchar,
    uri varchar,
    seoname varchar
);

create table features(
    id varchar,
    featureId varchar,
    duration varchar,
    time varchar
)

